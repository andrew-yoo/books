[project]
name = "books"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "duckdb>=1.4.4",
    "xxhash>=3.6.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
